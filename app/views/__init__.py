from __future__ import unicode_literals

from app.views.api.mobile import views as api_mobile_views
from app.views.api.users import views as api_users_views
from app.views.dashboard import views as dashboard_views
from app.views.index import index as app_index
from app.views.password_resets import views as password_reset_views
from app.views.sessions import views as sessions_views
from app.views.users import views as users_views
from app.views.users.benefit_forms import views as user_benefit_forms_views
from app.views.users.messages import views as user_messages_views
from app.views.users.paid_time_off import views as pto_views
from app.views.users.pay import views as user_messages_pay
from app.views.users.performance import views as user_performance_views
from app.views.users.retirement import views as user_retirement_views
from app.views.users.work_info import views as user_work_info_views
from app.views.admin import views as admin_views
